# 小河狸发票助手更新日志

## 1.2.1

- 加强导入方向识别：开具发票清单应表现为销售方主体集中，取得发票清单应表现为购买方主体集中。
- 当用户把开具清单选为取得导入，或把取得清单选为开具导入时，系统会拒绝导入且不写入数据。
- 导入其他公司数据时，如果当前公司/历史主体税号与文件主体税号不一致，系统会拒绝导入。
- 如果税号缺失、只能按名称判断主体异常，系统允许继续导入，但会以红色强提醒提示用户确认是否选错公司。

## 1.2.0

- 新增多公司管理，支持新增公司和修改公司名称。
- 数据导入按公司写入，发票唯一键调整为公司内去重和更新。
- 发票台账、辅助子表检索和分析看板支持公司选择。
- 分享链接支持选择单个公司或多个公司，并在令牌列表显示公司名称。
- AI 问答和本地统计按当前公司口径读取数据。
- 升级时自动修复历史数据批次归属，避免出现台账有数据但最近导入为空、无法删除或更新的情况。
- 新版启动时会优先关闭本机旧版服务，避免浏览器仍连接旧端口导致顶栏显示旧版本。

## 1.1.1

- 使用说明压缩为 6 张说明卡片。
- 分析看板 9 个指标调整为 3 张对比卡片。
- 趋势图和排行图支持金额单位自动换算，长名称截断并保留完整提示。
- AI 问答支持 Enter 发送、图表展示和 PDF 导出。
- 新增客户开票下降分析，优先使用本地真实统计。
- 上传控件、操作按钮、分享权限中文显示和 AI 窗口拖拽体验优化。

## 1.1.0

- 发票台账关键词检索支持状态和风险字段。
- 删除各页面重复说明，集中放入使用说明。
- 优化数据导入和分享链接表格操作按钮高度。
- AI 配置移动到提问上方，新增大模型连接测试。
- AI 回答区改为内部滚动，浮窗支持还原大小。

## 1.0.1

- 优化浅色模式标题、表头、下拉框和操作按钮显示。
- 分析看板改为仅按日期范围筛选，所有指标和图表统一剔除作废发票。
- 看板指标调整为开具、取得及差额的金额、税额、价税合计。
- AI 问答改为可拖动、可滚动浮窗，并优化大模型配置提示。
- 分享链接页面去除重复地址说明，保留生成后的可复制链接。
- 接入 GitHub 更新清单，用于发现新版并提醒升级。
